@page
@model final_project.Pages.Randomizer.ArmorModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayName("Armor")
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Armor)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Aname)
                </td>
                <td>
                    <a asp-page="./Edit" asp-route-id="@item.ArmorID">Edit</a> |
                    <a asp-page="./Delete" asp-route-id="@item.ArmorID">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
